@model CIR_skladisce.Models.UporabnikLogin

@{
    ViewBag.Title = "Log in";
}

<hgroup class="title">
    <h1>@ViewBag.Title.</h1>
</hgroup>

<section id="loginForm">
<h2>Use a local account to log in.</h2>
@using (Html.BeginForm(new { ReturnUrl = ViewBag.ReturnUrl })) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Log in Form</legend>
        <ol>
            <li>
                @Html.LabelFor(m => m.UporabniskoIme)
                @Html.TextBoxFor(m => m.UporabniskoIme)
                @Html.ValidationMessageFor(m => m.UporabniskoIme)
            </li>
            <li>
                @Html.LabelFor(m => m.Geslo)
                @Html.PasswordFor(m => m.Geslo)
                @Html.ValidationMessageFor(m => m.Geslo)
            </li>
        </ol>
        <input type="submit" value="Log in" />
    </fieldset>
    <p>
        @Html.ActionLink("Register", "Register") if you don't have an account.
    </p>
}
</section>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
